<% @categories.each do |category| %>
  <%= link_to category.title, category_path(category) %>
<% end %>

<% if current_user %>
  <% @items.each do |item| %>
    <p><%= item.title %> | $<%= to_currency(item.price) %> | <%= button_to 'Add to Cart', line_items_path(item_id: item) %></p>
  <% end %>
<% else %>
  <% @items.each do |item| %>
    <p><%= item.title %> | $<%= to_currency(item.price) %></p>
  <% end %>
<% end %>
